# скрипты bash запускаются в Linux (Colab), поэтому в них всегда переводы строк Linux
*.sh text eol=lf
# lock-файлы хранятся байт в байт такими, какими их записали программы
envs/* -text
# данные тоже хранятся байт в байт: блокнот F_M0 и тесты сверяют их с тем, что пишет программа
data/** -text
